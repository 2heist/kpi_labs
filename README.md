# Car Rental System

This project contains data models and UML diagrams for a car rental system.

## Project structure
- `docs/diagrams/` — PlantUML files (`.puml`) and images (`.png`)
- `src/` — source code
- `.gitignore` — ignored files list
- `README.md` — project info

